function payBillInit(){
  fromCardHeightPayBill = newBill.fromCard.height;
  toCardHeightPayBill = newBill.toCard.height;
}

function payBillPreShow(){
  /*
  	loadNewBillData();
  
    // setup toCard
  	newBill.toAccountPick.opacity = 0;
  	newBill.toAccountNameContainer.top = "100%";
    newBill.toAccountAmountContainer.top = "100%";
  	newBill.toLabel.left = "-25dp";
  	
  	// setup fromCard
  	newBill.fromAccountPick.opacity = 0;
  	newBill.fromAccountNameContainer.top = "100%";
    newBill.fromAccountAmountContainer.top = "100%";
  
  	// setup amountCard
  	newBill.amountPick.opacity = 0;
  	newBill.amountPickContainer.top = "100%";
  	*/
}